import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gh.edu.phoenixintlschool',
  appName: 'Phoenix International School',
  webDir: 'out'
};

export default config;
